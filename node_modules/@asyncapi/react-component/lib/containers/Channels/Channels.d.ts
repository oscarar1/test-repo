import React from 'react';
import { ExpandNestedConfig } from '../../config';
import { Channels } from '../../types';
interface Props {
    channels: Channels;
    expand?: ExpandNestedConfig;
}
export declare const ChannelsComponent: React.FunctionComponent<Props>;
export {};
