import React from 'react';
import { Operation, PayloadType } from '../../types';
interface Props {
    payloadType?: PayloadType;
    operation?: Operation;
    oneOf?: boolean;
    otherOneOf?: boolean;
    isPublish?: boolean;
    isSubscribe?: boolean;
}
export declare const OperationComponent: React.FunctionComponent<Props>;
export {};
