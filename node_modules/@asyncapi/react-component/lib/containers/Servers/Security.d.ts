import React from 'react';
import { Components, SecurityRequirement, ExcludeNullable } from '../../types';
interface Props {
    requirements: SecurityRequirement[];
    schemes: ExcludeNullable<Components['securitySchemes']>;
    openAccordion?: boolean;
    identifier: string;
    dataIdentifier: string;
}
export declare const ServerSecurityComponent: React.FunctionComponent<Props>;
export {};
