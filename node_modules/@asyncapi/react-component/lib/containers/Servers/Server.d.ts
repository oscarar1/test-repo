import React from 'react';
import { Server, SecurityScheme } from '../../types';
interface Props {
    server: Server;
    stage: string;
    securitySchemes?: Record<string, SecurityScheme>;
    toggleExpand?: boolean;
}
export declare const ServerComponent: React.FunctionComponent<Props>;
export {};
