import React from 'react';
import { OAuthFlow } from '../../types';
interface Props {
    name: string;
    flow: OAuthFlow;
}
export declare const ServerSecurityFlow: React.FunctionComponent<Props>;
export {};
