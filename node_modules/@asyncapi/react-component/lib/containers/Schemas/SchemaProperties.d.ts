import React from 'react';
import { Schema } from '../../types';
interface Props {
    name: string;
    hasDynamicName?: boolean;
    properties: Schema;
    treeSpace: number;
    description?: React.ReactNode;
    required?: boolean;
}
export declare const SchemaPropertiesComponent: React.FunctionComponent<Props>;
export {};
