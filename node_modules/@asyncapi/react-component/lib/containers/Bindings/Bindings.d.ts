import React from 'react';
import { BaseBindings } from '../../types';
interface Props {
    bindings?: BaseBindings;
    title?: string;
}
export declare const BindingsComponent: React.FunctionComponent<Props>;
export {};
