import React from 'react';
import { RawMessage } from '../../types';
interface Props extends Required<Pick<RawMessage, 'payload'>> {
    oneOf?: boolean;
    anyOf?: boolean;
    identifier?: string;
    dataIdentifier?: string;
    id?: number;
    examples?: object[];
}
export declare const PayloadComponent: React.FunctionComponent<Props>;
export {};
