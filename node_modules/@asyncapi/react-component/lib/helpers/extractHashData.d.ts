import { CONTAINER_LABELS } from '../constants';
export interface HashData {
    schema: string;
    container: CONTAINER_LABELS;
    item?: string;
}
export declare function extractHashData(hash: string): HashData | undefined;
