import { ParserReturn, FetchingSchemaInterface } from '../types';
export declare class Parser {
    private parseSchema;
    private parseSchemaFromURL;
    constructor();
    parse(content: string | any, parserOptions?: any): Promise<ParserReturn>;
    parseFromUrl(arg: FetchingSchemaInterface, parserOptions?: any): Promise<ParserReturn>;
    private handleError;
    private extractDocument;
}
