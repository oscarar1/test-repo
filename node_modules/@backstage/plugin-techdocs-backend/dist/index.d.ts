import { PluginEndpointDiscovery } from '@backstage/backend-common';
import { Config } from '@backstage/config';
import { PreparerBuilder, GeneratorBuilder, PublisherBase } from '@backstage/techdocs-common';
export * from '@backstage/techdocs-common';
import express from 'express';
import { Knex } from 'knex';
import { Logger } from 'winston';

declare type RouterOptions = {
    preparers: PreparerBuilder;
    generators: GeneratorBuilder;
    publisher: PublisherBase;
    logger: Logger;
    discovery: PluginEndpointDiscovery;
    database?: Knex;
    config: Config;
};
declare function createRouter({ preparers, generators, publisher, config, logger, discovery, }: RouterOptions): Promise<express.Router>;

export { createRouter };
